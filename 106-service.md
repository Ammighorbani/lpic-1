# Service

### What is service command?
**service is a command like systemctl and old sys admins use it at the begginig admins had service but after a time developers have been made [systemctl](104-systemctl.md) and you can read my document about `systemctl`**

### Note: service sub commnads are look like `systemctl`

---

### Example of service
```bash
service ssh status
```